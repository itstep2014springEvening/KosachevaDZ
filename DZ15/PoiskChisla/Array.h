#ifndef ARRAY_H_INCLUDED
#define ARRAY_H_INCLUDED

const int s=1000;
void input (int a[s], int size);
void output (int a[s], int size);
void searchelement (int a[s], int size, int n);

#endif // ARRAY_H_INCLUDED

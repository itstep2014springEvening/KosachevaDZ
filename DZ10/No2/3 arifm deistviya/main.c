#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b; //рандомные числа
    printf("%d + %d = %d\n", a, b, a + b);
    printf("%d - %d = %d\n", a, b, a - b);
    printf("%d * %d = %d\n", a, b, a * b);
    return 0;
}

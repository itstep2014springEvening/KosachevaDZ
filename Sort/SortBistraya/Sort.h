#ifndef SORT_H_INCLUDED
#define SORT_H_INCLUDED

const int N=10000;
void input (int a[N], int size);
void output (int a[N], int size);
void sort (int a[N], int first, int last);
void change (int index, int nextindex);

#endif // SORT_H_INCLUDED

#ifndef ARRAY_H_INCLUDED
#define ARRAY_H_INCLUDED

const int N=10000;
void input (int a[N], int size);
void output (int a[N], int size);
void sort (int a[N], int size);
void change (int a[N], int index, int lastindex);

#endif // ARRAY_H_INCLUDED
